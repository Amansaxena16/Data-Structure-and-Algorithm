// writing a program to reverse each word in the string!!
#include<iostream>
#include<cstring>
using namespace std;

char reverWord(string str){
    int len = strlen(str);
    char temp[len];
    int j = 0;

    for(int i = 0; str[i] != " " || '\0'; i++){
        temp[j] = 
    }
}
int main(){
    string str = "hey this is animation";

    reverWord(str);
    return 0;
}