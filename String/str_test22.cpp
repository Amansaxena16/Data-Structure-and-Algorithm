// writing a program to find the string is anagram or not!!  
#include<iostream>
using namespace std;

int findAnagram(char str1[], char str2[]){
    // Initializing two arrays
    int arr1[26] = {0}; 
    int arr2[26] = {0};

    // Length of both of the string
    int len1 = strlen(str1);
    int len2 = strlen(str2);
    
    // If length of both string don't match it will return 0
    if(len1 != len2){
        return 0;
    }

    
    else{
        for(int i = 0; str[i] != )
    }


    
}

int main(){
    
    char str1[10] = "triangle";
    char str2[10] = "integral";

    findAnagram(str1, str2);
    return 0;
}