#include<iostream>
using namespace std;

bool isPowerOfTwo(int n);
int main(){
    int n;
    cout << "Enter a Number to Check whether it is power of Two or Not!! ";
    cin >> n;
    bool isPowerOfTwo(n);
}

bool isPowerOfTwo(int n){
    int power = 1;
    if(n == 0){
        return true
    }

    while(){
        power = power * 2;
        if(power == )
    }
    
    if(power )
}